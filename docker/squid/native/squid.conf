#
# Squid 配置 — 原生 OpenClaw 代理（无 arsguard 过滤）
# 作用：将请求透传到 openclaw-native，不做安全拦截
# =============================================================

# 监听端口
http_port 3129 accel defaultsite=eval-ollama

# 日志
access_log /var/log/squid/access.log
cache_log /var/log/squid/cache.log
logfile_rotate 4

# 缓存配置
cache_mem 256 MB
maximum_object_size_in_memory 512 KB

# 转发配置：强制所有请求经过 Ollama 后端
cache_peer eval-ollama parent 11434 0 no-query originserver name=upstream
never_direct allow all

# 允许所有经过代理的请求（原生无过滤）
http_access allow all

# 性能优化
quick_abort_min -1 KB
forwarded_for delete
via off
